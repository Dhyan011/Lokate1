// Tailwind v4: theme values are configured via @theme in index.css
// This file only needs the content glob for class scanning.
export default {
  content: [
    './index.html',
    './src/**/*.{ts,tsx,js,jsx}',
  ],
}
